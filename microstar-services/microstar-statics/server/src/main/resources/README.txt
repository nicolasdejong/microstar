Resources can be downloaded from /public/ (which should not be part of the url).
Urls that try to point to different files in /resources will lead to a 404.

Resources from /default will be set as default contents of the /data/static-data/ folder.
